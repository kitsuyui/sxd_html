name: cargo-deny
on:
  pull_request:
  push:
    branches:
      - main
permissions:
  contents: read
jobs:
  deny:
    name: cargo-deny
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: EmbarkStudios/cargo-deny-action@v2
